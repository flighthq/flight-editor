# Flight Assets Upstream TODO

Ownership: Flight upstream (`@flighthq/assets`).

- Keep runtime asset descriptors, acquisition, caching, lifetime, missing-asset state, and disposal host-neutral.
- Expose dependency/reference metadata sufficient for editor usage queries and transfer planning.
- Define stable asset identity and replacement behavior across manifest reloads.
- Keep library-panel organization, thumbnails, import UX, master/instance overrides, and editor commands in Flight Editor.
- Test manifest replacement, missing dependencies, cancellation, and hot reload.

