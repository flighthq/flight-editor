# Editor Color TODO

Ownership: Flight Editor over Flight color/material primitives.

- Distinguish active literal colors, referenced color tokens, recent colors, and saved document swatches.
- Add gradient-stop editing, alpha, color-space metadata, and deterministic string parsing/formatting.
- Make eyedropper availability a host capability and one cancellable gesture.
- Define token update propagation and conversion from reference to literal.
- Test mixed values, gradient edits, invalid text drafts, token deletion, clipboard, and round trips.

