# Editor Version History Follow-up

Implemented immutable canonical revisions, validated lineage, storage adapters, read-only migrated previews, naming metadata, duplication, semantic comparison inputs, dirty-work cancel/discard/stash policy, and restore-as-new-revision.

Remaining: concrete local/service stores; format migration registry integration; preview diagnostics from asset/plugin resolution; host UI and failure recovery conformance.
