# Flight Materials Upstream TODO

Ownership: Flight upstream (`@flighthq/materials`).

- Expose stable material/property schemas, defaults, validation, renderer support, texture-slot types, and clone/reference behavior.
- Define color-space, alpha, sidedness, transparency ordering, and hot-reload semantics.
- Keep material library organization, thumbnails, inspector metadata, commands, and presets in Flight Editor.
- Test schema parity across renderers and deterministic unsupported-feature diagnostics.

