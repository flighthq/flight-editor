# Editor Export Settings TODO

Ownership: Flight Editor over host filesystem and Flight renderer/encoder capabilities.

- Attach export slices to stable node or artboard identities.
- Add deterministic scale sets, suffixes, background policy, clipping, naming conflicts, and batch ordering.
- Separate document-persisted export marks from transient export-dialog choices.
- Capability-gate formats and report unsupported renderer/encoder combinations before export.
- Test renamed/deleted targets, multiple scales, cancellation, partial failure, and cross-host parity.

