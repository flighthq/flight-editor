# Editor Transform Origin TODO

Ownership: Flight Editor, persisted authoring data through the Flight scene format where required.

- Distinguish the node registration point from the movable transform center.
- Define persistence, reset, snapping, multi-selection, nested-coordinate, and duplication behavior.
- Specify whether temporary aggregate-selection origins are document state.
- Ensure inspector coordinates and canvas handles use the same coordinate contract.
- Test rotated and scaled parents, negative scale, nested scopes, and reload round trips.

