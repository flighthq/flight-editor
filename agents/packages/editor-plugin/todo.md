# Editor Plugin Follow-up

Implemented versioned manifests, host capability enforcement, deterministic contributions, activation rollback, reverse-order disposal, command-gated document mutations, namespaced missing-plugin data, migrations, read-only generators, and declarative widget validation.

Remaining: host-owned installation/permissions; richer widget schemas; compose contribution adapters into each target; add sandbox enforcement where a host supports it.
