export {
  createDesktopCapabilities,
  createHeadlessAdapter,
  createHeadlessCapabilities,
  createHostAdapterState,
  getHostAdapter,
  getHostAdapterVersion,
  getHostCallbacks,
  getHostCapabilities,
  hasCapability,
  setHostAdapter,
  setHostCallbacks,
} from './hostAdapter';

export type { FileDialogResult, HostAdapter, HostAdapterState, HostCallbacks, HostCapabilities } from './hostAdapter';
