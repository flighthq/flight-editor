export {
  clearCursorPosition,
  clearStatusMessage,
  createStatusBarState,
  getActiveToolName,
  getCursorPosition,
  getSelectionCount,
  getSelectionLabel,
  getStatusBarVersion,
  getStatusMessage,
  getZoomPercent,
  setActiveToolName,
  setCursorPosition,
  setSelectionInfo,
  setStatusMessage,
  setZoomPercent,
} from './statusState';

export type { CursorPosition, MessageSeverity, StatusBarState, StatusMessage } from './statusState';
