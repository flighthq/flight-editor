export {
  createCanvasState,
  getBackgroundPattern,
  getCanvasVersion,
  getOverlayOpacity,
  getPixelRatio,
  isShowBounds,
  isShowGrid,
  isShowGuides,
  isShowPixelGrid,
  setBackgroundPattern,
  setOverlayOpacity,
  setPixelRatio,
  setShowBounds,
  setShowGrid,
  setShowGuides,
  setShowPixelGrid,
} from './canvasState';

export type { BackgroundPattern, CanvasState } from './canvasState';
